# CYROID Red Team - WordPress Target Template
seed_id: redteam-wordpress-target
name: "Red Team - WordPress Target"
os_family: ubuntu
os_version: "22.04"
description: |
  WordPress with vulnerable Acme Employee Portal plugin.
  Contains SQL injection and XSS vulnerabilities for training.

os_type: linux
os_variant: "Ubuntu 22.04"
vm_type: container
base_image: redteam-lab-wordpress:latest

default_cpu: 2
default_ram_mb: 2048
default_disk_gb: 20

native_arch: x86_64

config_script: "# WordPress auto-configures on startup"

tags:
  - red-team
  - victim
  - sqli
  - xss
  - wordpress
  - web-server
