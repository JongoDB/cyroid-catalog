# CYROID Ubuntu Server 22.04 LTS Template
seed_id: ubuntu-server-22.04
name: "Ubuntu Server 22.04"
os_family: ubuntu-server
os_version: "22.04"
description: |
  Ubuntu Server 22.04 LTS as a full QEMU virtual machine.
  Suitable for server workloads, web servers, databases, and services.

os_type: linux
os_variant: "Ubuntu Server 22.04 LTS"
vm_type: linux_vm
linux_distro: ubuntu
base_image: qemux/qemu:latest

default_cpu: 2
default_ram_mb: 2048
default_disk_gb: 20

boot_mode: uefi
disk_type: scsi

native_arch: both

tags:
  - server
  - ubuntu
  - linux
  - infrastructure
