name: redteam-vpn
tag: latest
description: |
  OpenVPN gateway for Red Team Training Lab v3.
  Dual-homed, accepts credentials found via SQLi.
container_config:
  privileged: true
  cap_add:
    - NET_ADMIN
