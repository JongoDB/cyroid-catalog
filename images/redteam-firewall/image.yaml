name: redteam-firewall
tag: latest
description: |
  Multi-homed firewall/router for Red Team Training Lab v3.
  Provides NAT and access control between network segments.
container_config:
  privileged: true
  cap_add:
    - NET_ADMIN
    - NET_RAW
